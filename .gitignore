.vscode
.DS_Store
__pycache__
build/
dist/**
entry/data/
*.dol
*.elf
*.iso
*.gz

ipl.rom
*.bin
*.bin.scrambled

patches/include/secret.h
patches/tests

gbi.hdr

# Build outputs that land in the repo root (all regenerated by .ci/*.sh).
apploader.img
*.uf2
EXTRACT_TO_ROOT.zip
cubiboot-dist.zip

# Generated: entry/Makefile copies cubeboot/source/emu here before building
# patches, and deletes it again on `make clean`. The sources live in
# cubeboot/source/emu -- never commit this copy, it goes stale silently.
patches/source/emu/
cubeboot/data/
